if [ -d "INPUT_DATM" ]; then
  echo "INPUT_DATM/ directory exists ..."
else
  echo "INPUT_DATM/ directory not found. Creating ..."
  mkdir -p INPUT_DATM
fi

cd INPUT_DATM
rsync -arv @[RTPWD]/DATM_ERA5_input_data/. . 
cd -
