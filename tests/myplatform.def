# tests/myplatform.def -- community-platform definition template for rt.sh
#
# Usage:
#   1. Copy this file (keep the original as a reference) and edit the four
#      data lines below for your own platform.
#   2. Tag the rt.conf (or -l <file>) COMPILE/RUN lines you want to run on
#      this platform with "+<PLATFORM_NAME>" in their Machines column,
#      using the same name given on header line 1 below. "-<PLATFORM_NAME>"
#      excludes a line from this platform even if "+<PLATFORM_NAME>" is
#      also present.
#   3. Run:
#        ./rt.sh -a <account> -P myplatform.def -l rt.conf
#
# Exactly four data lines are read here, in that order, skipping blank
# lines and comment lines ("#", like this one). A list of tests is given
# in rt.conf. A community-platform run is always sequential (no -r/-e)
# and always skips baseline comparison (no -c/-m): it is a portability
# check ("does the model build and run here at all?"), not a regression
# test. See the User's Guide chapter "Container and Community Platform
# Options" for the full field reference.
# ------------------------------------------------------------------------
# Header line 1: PLATFORM_NAME | RT_COMPILER | CONTAINER_IMG | BIND_DIRS
#
#   PLATFORM_NAME  Required. Becomes MACHINE_ID for this run, and the name
#                  used in the "+<name>"/"-<name>" rt.conf tag above. May
#                  be "container" to reuse the repository's existing
#                  container job-card templates and modulefile as-is.
#   RT_COMPILER    Required. This platform's one and only compiler:
#                  "intel" or "gnu". rt.conf lines tagged for this
#                  platform but built with the other compiler are skipped
#                  with a notice, not an error.
#   CONTAINER_IMG  Absolute path to a Singularity/Apptainer image (*.sif)
#                  on this host. Leave blank to build/run against a
#                  native software stack instead -- see
#                  modulefiles/ufs_<PLATFORM_NAME>.<compiler>.lua.
#   BIND_DIRS      Comma-separated host directories to bind-mount into the
#                  container (must cover the source tree, input data, and
#                  RUNDIR_ROOT below). Ignored for a native stack.
# ------------------------------------------------------------------------
# Header line 2: TPN | SCHEDULER | ACCNR | PARTITION | QUEUE | MPI_LAUNCH
#
#   TPN         Required. MPI tasks per node on this platform.
#   SCHEDULER   "slurm", "pbs", or blank for an interactive/no-scheduler
#                run.
#   ACCNR       Scheduler account/project name (may also be given with
#               rt.sh's own -a; a value here is used if -a is omitted).
#   PARTITION   Slurm partition name (blank for PBS or no scheduler).
#   QUEUE       Slurm QOS / PBS queue name (blank for no scheduler).
#   MPI_LAUNCH  MPI launch command used only when SCHEDULER is blank:
#               "mpirun" or "mpiexec" (defaults to "mpirun" if omitted).
# ------------------------------------------------------------------------
# Header line 3: RUNDIR_ROOT
#
#   Required. Top-level directory where compile and test run directories
#   are created. Should be a user-writable path, preferably on a scratch
#   or work filesystem.
# ------------------------------------------------------------------------
# Header line 4: INPUTDATA_ROOT | INPUTDATA_ROOT_WW3 | INPUTDATA_LM4 | INPUTDATA_GFSv17opn
#
#   INPUTDATA_ROOT       Required. Staged input-data directory (fix files
#                        and other RT input data).
#   INPUTDATA_ROOT_WW3   Optional. Defaults to a WW3_input_data_* sibling
#                        directory under INPUTDATA_ROOT if left blank.
#   INPUTDATA_LM4        Optional. Defaults to an LM4_input_data
#                        subdirectory of INPUTDATA_ROOT if left blank.
#   INPUTDATA_GFSv17opn  Optional. Only needed by tests that explicitly
#                        require GFSv17 operational input data.
# ------------------------------------------------------------------------

myplatform | intel | /path/to/containers/rocky9-oneapi2024.2-ss192.sif | /path1,/path2
40 | slurm | myaccount | mypartition | myqueue | mpirun
/path/to/runtime_dir
/path/to/input-data | | |
